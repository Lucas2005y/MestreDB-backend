# ===========================================
# CONFIGURAÇÕES DO SERVIDOR
# ===========================================
PORT=3000
NODE_ENV=development

# ===========================================
# MYSQL (Docker usa porta 3307)
# ===========================================
MYSQL_HOST=localhost
MYSQL_PORT=3307
MYSQL_USERNAME=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=mestredb_sql

# ===========================================
# JWT - IMPORTANTE: Altere em produção!
# ===========================================
# O código lê JWT_EXPIRES_IN (não JWT_ACCESS_EXPIRES_IN)
JWT_SECRET=seu_jwt_secret_super_seguro_aqui_com_pelo_menos_32_caracteres
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# ===========================================
# CORS
# ===========================================
CORS_ORIGIN=http://localhost:3000

# ===========================================
# USUÁRIO ADMINISTRADOR PADRÃO
# ===========================================
ADMIN_EMAIL=admin@mestredb.com
ADMIN_PASSWORD=MinhaSenh@123

# ===========================================
# RATE LIMITING - Proteção contra força bruta
# ===========================================
RATE_LIMIT_MAX_ATTEMPTS=5
RATE_LIMIT_WINDOW_MINUTES=15
RATE_LIMIT_BLOCK_MINUTES=15

# ===========================================
# LOGGING (Opcional)
# ===========================================
# LOG_LEVEL=info

# ===========================================
# NOTAS IMPORTANTES:
# ===========================================
# 1. MongoDB foi removido - projeto usa apenas MySQL
# 2. Docker mapeia MySQL para porta 3307 (não 3306)
# 3. JWT_EXPIRES_IN é a variável correta (não JWT_ACCESS_EXPIRES_IN)
# 4. Em produção, use JWT_SECRET com 64+ caracteres aleatórios
# 5. Em produção, altere ADMIN_PASSWORD para senha forte e única
# 6. Em produção, configure CORS_ORIGIN com domínio real