PORT=3001
NODE_ENV=test

# MySQL (testes usam SQLite em memória, mas mantemos config para compatibilidade)
MYSQL_HOST=localhost
MYSQL_PORT=3307
MYSQL_USERNAME=root
MYSQL_PASSWORD=root
MYSQL_DATABASE=mestredb_test

# JWT (secret específico para testes)
JWT_SECRET=test-secret-key-for-automated-tests-only-min-32-chars
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3001

# Admin padrão para testes
ADMIN_EMAIL=admin@test.com
ADMIN_PASSWORD=TestPassword123

# Rate Limiting (mais permissivo para testes)
RATE_LIMIT_MAX_ATTEMPTS=100
RATE_LIMIT_WINDOW_MINUTES=1
RATE_LIMIT_BLOCK_MINUTES=1
